version: '3.9'
services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    ports:
      - '8080:3000'
    networks:
      - services

networks:
  services:
    external: true
